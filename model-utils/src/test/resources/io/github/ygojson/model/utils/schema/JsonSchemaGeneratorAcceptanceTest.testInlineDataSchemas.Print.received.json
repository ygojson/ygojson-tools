{
  "$schema" : "https://json-schema.org/draft/2019-09/schema",
  "type" : "object",
  "properties" : {
    "id" : {
      "type" : "string",
      "format" : "uuid",
      "description" : "UID (v5) for the Print (atomic) generated by YGOJSON."
    },
    "cardId" : {
      "type" : "string",
      "format" : "uuid",
      "description" : "UID (v5) for the Card (atomic) associated with this print (generated by YGOJSON)."
    },
    "setId" : {
      "type" : "string",
      "format" : "uuid",
      "description" : "UID (v5) for the Set (atomic) associated with this print (generated by YGOJSON)."
    },
    "setPrefix" : {
      "type" : "string",
      "description" : "Set prefix associated with this print.\n\nThis property might differ with the prefix on the Set Model as the print-codes are often localized.",
      "pattern" : "[a-zA-Z0-9]+-[a-zA-Z0-9]+"
    },
    "printNumber" : {
      "type" : "string",
      "description" : "Number of the print within the set.\n\nIt is represented as a string with left-padded zeroes, although sometimes might contain a letter.",
      "pattern" : "[a-zA-Z0-9]+"
    },
    "rarity" : {
      "type" : "string",
      "description" : "Rarity of the print",
      "pattern" : "[a-z]+"
    },
    "language" : {
      "type" : "string",
      "enum" : [ "ja", "es" ],
      "description" : "Language of the print"
    }
  },
  "required" : [ "id" ],
  "$id" : "io.github.ygojson.model.data.Print",
  "description" : "Describes the information for a print.\n\nThis model contains the properties of the printed card that will not be common to all of them."
}