coverage:
  precision: 3
  round: nearest
  range: "50...100"
  status:
    project:
      default:
        # TODO: change when we have a stable release
        target: 60%
    patch:
      default:
        # TODO: change when we have a stable release
        target: 60%

comment:
  behavior: new
